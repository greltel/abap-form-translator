@EndUserText.label: 'Access control for ZI_FORM_NAME_VH'
@MappingRole: true
define role ZI_FORM_NAME_VH {
  grant
    select
      on
        ZI_FORM_NAME_VH
          where
            ( ) = aspect pfcg_auth( ZFORMTRA, ACTVT = '03' );
}